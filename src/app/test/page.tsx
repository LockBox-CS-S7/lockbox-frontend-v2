import { FileUpload } from "@/components/file-upload";

export default function Test() {
    return (
        <div className="flex items-center justify-items-center min-w-full min-h-full">
            <FileUpload />
        </div>
    );
}
